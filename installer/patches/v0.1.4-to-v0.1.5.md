---
patch_id: root-engineering-0.1.4-to-0.1.5
target_package: root-engineering-chat-installer
from_version: 0.1.4
to_version: 0.1.5
schema_version: 0.1.0
requires_exact_from_version: true
idempotent: true
touches:
  - Global Protocol
  - Project Instructions
  - Global Manifest package version
  - Project Manifest package version
must_not_touch:
  - document and folder IDs
  - Foundation content
  - Current Knowledge content
  - Learned Knowledge content
  - History content
  - Sources and Skills content
next_patch: null
---

# Patch 0.1.4 → 0.1.5: Explicit Update Routing

## Preconditions

- Require Package ID `root-engineering-chat-installer` and Package Version `0.1.4` in both Manifests.
- Require one unambiguous Project Binding and readable Global Protocol and Project Instructions.
- If either version differs, stop without mutation.

## Operations

Add one `Versioned Upgrade Routing` section to the Global Protocol and Project Instructions. If a partial section exists, replace it rather than duplicating it.

The section must state:

1. Existing installations use `ROOT_ENGINEERING_UPDATER.md` and the canonical patch registry.
2. Updater URL: `https://raw.githubusercontent.com/Valon-Jang/Root-Engineering/main/installer/ROOT_ENGINEERING_UPDATER.md`.
3. Registry URL: `https://raw.githubusercontent.com/Valon-Jang/Root-Engineering/main/installer/patches/README.md`.
4. Read the exact Package Version from both Manifests before selecting a patch; the values must match.
5. Resolve only a contiguous forward-only chain whose next `from_version` exactly equals the verified current version.
6. Read, apply, and verify one patch at a time. Update both Manifest versions only after that patch passes.
7. Never skip, merge, invent, or reorder patches. Never downgrade or use fresh INSTALL as an update substitute.
8. If the installed version or a required transition cannot be proven, stop and report the exact missing transition without mutation.
9. Do not permanently attach updater, registry, or patch files as Project Sources.

Do not copy the historical patch bodies into either installed document. The installed rule contains routing only; the registry and versioned patch files remain the single source for update steps.

## Verification

- Both installed documents contain one update-routing section and the two exact URLs.
- The section requires exact matching, contiguous forward-only patches, and per-patch verification.
- It contains explicit no-skip, no-invention, no-downgrade, no-fresh-install, and stop conditions.
- No historical patch instructions were embedded or duplicated.
- Existing IDs and project knowledge are unchanged.

Only after all checks pass, set Package Version to `0.1.5` in both Manifests and re-read both values. Keep Schema Version `0.1.0`.

## Failure

On failure, leave both Manifest versions at `0.1.4`, identify the failed scope or missing transition, and stop.
